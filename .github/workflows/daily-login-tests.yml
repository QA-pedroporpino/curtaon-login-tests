# =================================================================
# Workflow de Testes Automatizados - CurtaON
# Mantido por: Time de QA
# Última atualização: Fevereiro 2024
# =================================================================

name: Daily Login Tests

# Definição dos triggers do workflow
on:
  # Execução agendada - 12:00 UTC
  schedule:
    - cron: "0 12 * * *"
  # Permite execução manual via UI
  workflow_dispatch:
    inputs:
      debug_enabled:
        description: "Habilitar modo debug com logs detalhados"
        required: false
        default: false
        type: boolean
      test_filter:
        description: "Filtro de testes a serem executados (opcional)"
        required: false
        type: string
        default: ""

# Configurações globais e permissões
permissions:
  contents: read
  actions: write
  checks: write # Necessário para publicar resultados de teste

# Variáveis de ambiente globais
env:
  PYTHON_VERSION: "3.11"
  PYTHONUNBUFFERED: "1" # Força output sem buffer
  FORCE_COLOR: "1" # Força saída colorida
  DEBIAN_FRONTEND: "noninteractive"
  # Configurações do Chrome
  DISPLAY: ":99"
  CHROME_BIN: "/usr/bin/google-chrome"
  # Diretórios do projeto
  PROJECT_ROOT: "."
  TESTS_DIR: "tests/test_cases"
  ARTIFACTS_DIR: "artifacts"
  REPORTS_DIR: "tests/reports"
  HTML_DIR: "artifacts/html"
  SCREENSHOTS_DIR: tests/screenshots
  LOGS_DIR: tests/logs
  # Configurações de debug
  DEBUG: ${{ github.event.inputs.debug_enabled == 'true' }}
  # Configurações de cache
  CACHE_VERSION: "v1"
  PYTHONPATH: ${{ github.workspace }}

jobs:
  test:
    name: Executar Suite de Testes
    # Usando Ubuntu 22.04 para garantir compatibilidade com libgconf-2-4
    runs-on: ubuntu-22.04
    timeout-minutes: 30 # Aumentado para evitar timeouts

    steps:
      # ==== SETUP DO AMBIENTE ====
      - name: Checkout do código
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Setup Python ${{ env.PYTHON_VERSION }}
        id: setup-python
        uses: actions/setup-python@v4
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: "pip"
          cache-dependency-path: |
            requirements.txt
            setup.py

      - name: Criar ambiente virtual e instalar dependências
        run: |
          python -m venv venv
          source venv/bin/activate
          sudo apt-get update
          sudo apt-get install -y xvfb libgconf-2-4 chromium-browser
          pip install --upgrade pip
          pip install -r tests/test_cases/requirements.txt
          pip install pytest-xvfb pytest-html pytest-xdist

      - name: Criar diretórios necessários
        run: |
          mkdir -p ${{ env.SCREENSHOTS_DIR }}
          mkdir -p ${{ env.LOGS_DIR }}
          mkdir -p ${{ env.REPORTS_DIR }}

      # ==== SETUP DO CHROME E XVFB ====
      - name: Setup Chrome
        uses: browser-actions/setup-chrome@latest
        with:
          chrome-version: stable

      - name: Start Xvfb
        run: |
          Xvfb :99 -screen 0 1920x1080x24 > /dev/null 2>&1 &
          echo "DISPLAY=:99" >> $GITHUB_ENV

      - name: Wait for Xvfb
        run: sleep 3

      # ==== EXECUÇÃO DOS TESTES ====
      - name: Executar testes
        run: |
          # Ativa o ambiente virtual
          source venv/bin/activate

          # Configura display para Xvfb
          export DISPLAY=:99

          # Executa os testes com mais informações de debug
          pytest tests/test_cases/ \
            -v \
            --html=${{ env.REPORTS_DIR }}/report.html \
            --self-contained-html \
            --junitxml=${{ env.REPORTS_DIR }}/junit.xml \
            --log-cli-level=INFO \
            --log-file=${{ env.LOGS_DIR }}/pytest.log

      # ==== PUBLICAÇÃO DE ARTEFATOS ====
      - name: Publicar relatório HTML
        if: always()
        uses: actions/upload-artifact@v3
        with:
          name: test-report
          path: ${{ env.REPORTS_DIR }}
          retention-days: 7

      - name: Publicar screenshots
        if: always()
        uses: actions/upload-artifact@v3
        with:
          name: screenshots
          path: ${{ env.SCREENSHOTS_DIR }}
          retention-days: 7

      - name: Publicar logs
        if: always()
        uses: actions/upload-artifact@v3
        with:
          name: logs
          path: ${{ env.LOGS_DIR }}
          retention-days: 7

      # ==== PUBLICAÇÃO DE RESULTADOS ====
      - name: Publicar resultados dos testes
        if: always()
        uses: EnricoMi/publish-unit-test-result-action@v2
        with:
          files: "${{ env.REPORTS_DIR }}/junit.xml"
          comment_mode: off

      # ==== DEBUG FINAL ====
      - name: Debug final
        if: always()
        run: |
          echo "::group::📜 Log do Pytest"
          cat "${LOGS_DIR}/pytest.log" || echo "Log não encontrado"
          echo "::endgroup::"

          echo "::group::🧩 Variáveis de Ambiente"
          printenv | sort
          echo "::endgroup::"

          echo "::group::🐍 Dependências Python"
          pip list --format=columns
          echo "::endgroup::"

          echo "::group::🔍 Estrutura Final"
          ls -R
          echo "::endgroup::"

      # ==== CLEANUP ====
      - name: Cleanup
        if: always()
        run: |
          set -eo pipefail

          echo "::group::🧹 Limpando ambiente"
          if [ -f /tmp/xvfb.pid ]; then
            kill $(cat /tmp/xvfb.pid) || true
            rm /tmp/xvfb.pid
          fi
          pkill Xvfb || true
          sudo apt-get clean
          sudo rm -rf /var/lib/apt/lists/*
          echo "::endgroup::"

          rm -rf venv
